# ─── Broker y datos de mercado ────────────────────────────────────────────────
alpaca-py>=0.30.0

# ─── Procesamiento de datos ───────────────────────────────────────────────────
pandas>=2.0.0
# pandas-ta: opcional, los indicadores SMA/RSI estan implementados en pandas puro
# (pandas-ta requiere numba, que no soporta Python 3.14+)
# pandas-ta>=0.3.14b0

# ─── Configuracion de entorno ─────────────────────────────────────────────────
python-dotenv>=1.0.0

# ─── Planificador de tareas ───────────────────────────────────────────────────
schedule>=1.2.0

# ─── Zonas horarias ───────────────────────────────────────────────────────────
pytz>=2024.1

# ─── Pruebas unitarias ────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# ─── Dashboard ────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0

# --- Notificaciones ---------------------------------------------------------
requests>=2.31.0

# ─── Machine Learning (Meta-Labeling — Capa 2) ───────────────────────────────
# LightGBM: clasificador binario para predecir P(TP antes de SL).
lightgbm>=4.0.0

# scikit-learn: métricas de evaluación (AUC-ROC, F1) y TimeSeriesSplit.
scikit-learn>=1.3.0

# numpy: operaciones vectorizadas en features.py y labeling.py.
numpy>=1.26.0

# shap: interpretabilidad del modelo (feature importances avanzadas).
# Opcional — solo requerido para análisis, no para producción.
shap>=0.43.0
